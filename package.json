{
  "name": "node angular es6 basesetup",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && bower install && gulp build && node app.js"
  },
  "author": "Sebastian Griesa",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-concat": "^2.6.0",
    "gulp-install": "^0.6.0",
    "gulp-watch": "^4.3.10"
  }
}
